name: Test Action
on:
  workflow_dispatch:
  repository_dispatch:
    types: [dependency_update]
      
jobs:
  hello:
    runs-on: ubantu-latest
    steps:
      - name: hello
        run: echo "Hello world"
        shell: bash
