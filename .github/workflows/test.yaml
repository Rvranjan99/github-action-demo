name: Test Action
on:
  workflow_dispatch:
  
    inputs:
      payload:
        description: "snow payload"
        required: true
        default: '{}'
      input1: 
        description: "test"
        required: false
        default: "input1"
    
      
jobs:
  hello:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.1
      - name: hello
        run: echo "hello world!"
        shell: bash
      - name: Process Webhook
        id: webhook 
        run: |
          input_data=${{ toJSON(github.event.inputs.payload)}}
          parsed_data=$(echo $input_data | jq -r .)
          key_value=$(echo $parsed_data | jq -r .key)
          
          
          
          
          echo 'Payload - $key_value'
